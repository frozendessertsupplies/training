<h3>{{title}}</h3>
Type: {{question_type}} </br>
<h4>{{question}}</h4>

{{#if answers}}
    Answers: </br>
    {{#each answers}}
        <input type="radio" name="{{id}}" value="{{this}}"> {{this}}
    {{/each}}
{{else}}
    <input type="text" id="{{id}}">
{{/if}}

The Answer: {{answer}} </br>
